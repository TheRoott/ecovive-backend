package com.example.myapplication

object BuildConfig {
    // Esta clase será reemplazada automáticamente por el BuildConfig generado por Gradle
    // Solo se usa como fallback para desarrollo
    const val GOOGLE_MAPS_API_KEY = "demo_key_for_development"
}

